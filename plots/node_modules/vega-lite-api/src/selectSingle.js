import {set} from './__util__.js';
import {selectPoint} from './selectPoint.js';

export function selectSingle(...args) {
  const obj = selectPoint(...args);
  set(obj, "toggle", false);
  return obj;
}