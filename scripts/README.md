To be able to run the HPC job script, you will need to install MuJoCo in your home folder first:
```bash
mkdir -p $HOME/.local
cd $HOME/.local

wget https://github.com/google-deepmind/mujoco/releases/download/3.1.6/mujoco-3.1.6-linux-x86_64.tar.gz
tar -xzf mujoco-3.1.6-linux-x86_64.tar.gz
``` 
